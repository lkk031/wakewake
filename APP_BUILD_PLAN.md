# 日程与待办应用构建规划

> 文档状态：V1.1（已批准的个人日用 V1 基线）  
> 首要平台：Android  
> 核心能力：单机事项管理、日历与议程、本地通知、受控重复、JSON 备份恢复

---

## 1. 文档目的

本文档统一个人日用 V1 的产品、设计、客户端和测试口径，并作为任务拆分和验收依据。若旧设计与本文冲突，以本文和已采纳 ADR 为准。

V1 优先验证三个核心价值：

1. 用户能在一台 Android 设备上快速看清“哪天、几点、要做什么”。
2. 用户能在没有账号和网络的情况下创建、调整、完成并恢复事项。
3. 用户能按明确规则收到本地提醒，并通过 JSON 文件自行备份和恢复数据。

---

## 2. 产品定义与范围

### 2.1 产品定位

WakeWake 是 Android 优先的个人日程与待办应用。V1 面向单个设备上的单个用户，以 SQLite 为本地事实来源，不依赖登录、云端或远程推送。

React Native/Expo 继续保持 iOS 兼容性，但 V1 的功能、性能和发布验收以 Android 真机为先；Web 只用于开发预览，不是发布目标。

### 2.2 V1 必须包含

- 无账号进入应用，单用户本地使用。
- 创建、查看、编辑、完成、恢复和删除事项。
- 普通定时、仅截止、全天和无日期事项。
- 月、周、日、议程和收集箱视图。
- 分类、受控颜色、四级优先级和简短备注。
- 每项可配置多条本地提醒。
- 每日或每周重复，固定 `interval = 1`，永不结束。
- 通知权限说明、状态诊断、测试通知、调度与撤销对账。
- 用户主动导出和导入版本化 JSON 备份。
- SQLite Schema 迁移、备份导入校验、领域单元测试和 Android 真机关键流程。

### 2.3 V1 明确不包含

- 注册、登录、会话恢复、账号资料和账号删除。
- Supabase 云端保存、跨设备同步、Realtime、Outbox 和冲突处理。
- 远程推送、Push Token、服务端通知 worker 和投递回执。
- Android 精确闹钟权限及依赖精确闹钟的功能承诺。
- 每 N 天/周（N > 1）、每月、每年、自定义重复及重复结束条件。
- “本次及以后”拆分系列，以及直接修改既有系列的时间锚点。
- 团队协作、共享、指派、评论、附件、子任务和富文本。
- 第三方日历同步、AI 排程、地理围栏、桌面端、Web 端和穿戴设备端。

仓库中的 `supabase/` 配置、迁移和 RLS 测试作为 V1 后续能力的骨架继续保留，但不得进入 V1 运行链路，也不得成为本地功能验收前置条件。

---

## 3. 核心概念和业务规则

### 3.1 事项类型

1. **定时事项**：具有开始时间，可选截止时间，显示在时间轴中。
2. **截止事项**：只有截止时间，在对应日期和议程中展示。
3. **全天事项**：保存一个本地日历日期，不因 UTC 换算移动日期。
4. **无日期事项**：暂存于收集箱；没有时间基准时不能设置通知。
5. **重复事项**：按受控系列规则按需展开逻辑实例。

### 3.2 状态与操作

- 普通事项状态为 `open` 或 `completed`；删除使用可恢复窗口或软删除语义。
- 完成事项后保留在原日期，可过滤或折叠，并可恢复。
- 重复事项按实例记录完成状态，完成一次不结束整个系列。
- 逾期是根据当前时间和截止时间计算的派生状态，不单独存储。
- 危险操作优先提供短暂撤销；JSON 导入替换现有数据前必须明确确认。

### 3.3 分类与优先级

- 每个事项最多属于一个分类；分类包含名称、颜色、排序值和归档状态。
- 分类色、优先级和完成状态均不能只靠颜色表达。
- 优先级固定为 `none`、`low`、`medium`、`high`。

### 3.4 重复规则

V1 仅允许：

- 每日：每天，`interval = 1`。
- 每周：每周，`interval = 1`，选择一个或多个星期。
- 两者均永不结束，不接受 `until` 或 `count`。

系列保存本地日期/墙上时间与 IANA 时区锚点。系列创建后，锚点日期、时间、时区、频率和每周星期集合锁定。单次完成或编辑不能改变系列；若用户要修改锁定字段，V1 引导删除原系列并创建新系列。历史实例键和完成记录不能被静默改写。

跨夏令时展开保持锚点时区中的墙上时间，不使用固定 UTC 间隔。完整定义见 `docs/adr/0003-time-recurrence-notification-semantics.md`。

### 3.5 提醒规则

- 创建事项时复制全局默认提醒模板；之后修改模板不追溯已有事项。
- 每项支持 0 至 10 条提醒；偏移范围为 0 至 525600 分钟。
- 非全天事项以截止时间为基准；没有截止时间时以开始时间为基准。
- 全天事项以事项日期的**设备当地 09:00** 为基准，提前量从 09:00 向前计算。
- 无任何日期和时间的事项不能设置通知，界面应说明原因。
- 已过去的提醒不补发；编辑后只调度未来提醒。
- 事项完成、取消或删除后立即撤销尚未触发的提醒。
- 系统时区变化后，尚未触发的全天提醒按新的设备当地 09:00 重新计算。

---

## 4. 信息架构与页面

### 4.1 底部导航

1. **日历**：月、周、日视图切换。
2. **议程**：按日期分组的连续事项列表。
3. **收集箱**：无日期事项及快速整理。
4. **设置**：分类、默认提醒、通知诊断、JSON 备份与应用信息。

全局“＋”入口用于快速新建。

### 4.2 首次使用与通知权限

- 首次使用直接进入本地应用，不出现登录页。
- 不在启动第一屏直接请求系统通知权限。
- 先解释本地提醒的用途和系统限制，用户点击“开启提醒”后再请求权限。
- 拒绝后事项管理和 JSON 备份仍可使用；设置页提供系统权限入口。

### 4.3 核心视图

- **月视图**：展示事项数量和有限色标，点击日期进入该日。
- **周/日视图**：全天事项位于顶部，定时事项进入纵向时间轴。
- **议程**：按日期分组，逾期事项明确展示，支持完成、恢复和编辑。
- **收集箱**：集中展示无日期事项并提供安排日期入口。
- **快速创建/编辑**：首屏包含标题、日期、全天、开始/截止、分类和优先级；更多选项包含重复、提醒和备注。
- **设置**：分类、默认事项时长、默认提醒、通知权限/渠道、测试通知、导出备份和导入备份。

保存成功后必须立即更新 SQLite 和界面。V1 不显示同步状态，也不出现等待上传、账号过期或多设备冲突文案。

---

## 5. 技术方案与分层

| 层级         | V1 技术                                       | 用途                                   |
| ------------ | --------------------------------------------- | -------------------------------------- |
| 移动端       | React Native + Expo + TypeScript              | Android 优先，保留 iOS 兼容性          |
| 路由         | Expo Router                                   | 文件式路由与通知深链                   |
| 本地数据库   | Expo SQLite + Drizzle ORM                     | 单用户业务数据、迁移和结构化查询       |
| 本地 UI 状态 | Zustand                                       | 编辑草稿、筛选和临时交互               |
| 表单校验     | React Hook Form + Zod                         | 表单与备份导入的共享约束               |
| 日期时间     | date-fns + date-fns-tz                        | 时间格式化、时区与本地日期计算         |
| 重复规则     | 共享 Domain 受控实现                          | 每日/每周、`interval = 1` 的按需展开   |
| 通知         | expo-notifications                            | 本地通知调度、撤销、权限和点击路由     |
| 备份         | 版本化 JSON + 系统文件/分享能力               | 用户主动导出和事务性恢复               |
| 测试         | Vitest、React Native Testing Library、Maestro | 单元、组件和 Android 端到端测试        |
| 后续骨架     | Supabase                                      | 保留配置、迁移和 RLS，不接入 V1 运行时 |

分层约束：

- 页面只组合 Feature，不直接执行 SQL 或连接 Supabase。
- `packages/domain` 存放纯业务规则，不依赖 React Native。
- Repository 是 SQLite 业务读写的唯一入口；关联写入使用事务。
- 通知调度只能由通知服务调用，不散落在页面组件中。
- JSON 序列化、版本迁移和导入校验使用独立备份服务；不得导出缓存或原生通知 ID。
- V1 代码不得要求 Supabase 环境变量、网络或 Docker 才能启动核心流程。

---

## 6. 本地数据与 JSON 备份

### 6.1 SQLite 数据模型

V1 至少包含：

- `todos`：稳定 UUID、标题、备注、分类、优先级、事项类型、日期/时间、时区、状态和时间戳。
- `categories`：名称、受控色板 Token、排序和归档状态。
- `recurrence_rules`：仅保存受控频率、固定 interval、每周星期集合和锁定锚点。
- `occurrence_overrides`：重复实例的完成、恢复或取消记录，实例键必须稳定。
- `reminder_rules`：事项提醒偏移和排序。
- `settings`：默认时长、默认提醒、周起始日及本地偏好。
- `scheduled_notifications`：逻辑提醒键到原生通知 ID 的可重建映射。

约束：

- 标题去除首尾空格后不得为空。
- 全天事项保存本地 `YYYY-MM-DD`，不得伪装成 UTC 午夜。
- 非全天事项至少有开始或截止时间，同时存在时截止不得早于开始。
- 重复规则只接受 ADR 0003 的受控集合。
- 所有 Schema 变更必须提供可测试的本地迁移。

### 6.2 JSON 导出

- 顶层包含格式标识、`schemaVersion`、导出时间、应用版本和业务数据集合。
- 导出事项、分类、重复/实例记录、提醒规则和恢复所需设置。
- 不导出 SQLite 内部元数据、原生通知 ID、缓存、日志、崩溃信息或 Supabase 配置。
- 导出使用临时文件时，在分享/保存结束后尽快清理应用缓存副本。
- 文件可能包含敏感事项正文，导出前应明确提醒用户妥善保管。

### 6.3 JSON 导入

1. 使用系统文件选择器读取用户选择的文件。
2. 校验文件标识、支持的版本、大小上限、JSON 结构和全部领域约束。
3. 展示数据摘要并确认是否替换当前本地数据。
4. 在单个 SQLite 事务中导入；任何失败均回滚。
5. 提交成功后清除旧调度映射并执行通知对账。

V1 不承诺自动备份、增量备份、合并导入或跨设备实时同步。

---

## 7. 日期、时区与通知设计

### 7.1 日期与时区

- 非全天事项保存实际瞬间和相关 IANA 时区。
- 全天事项保存本地日期，不因旅行或 UTC 偏移改变日期。
- 重复系列按锁定的墙上时间和 IANA 时区展开；DST 后仍保持约定墙上时间。
- DST 导致本地时间不存在时移动到下一个有效时间；回拨重复时间默认取第一次出现的瞬间。
- 应用启动、恢复前台和系统时区变化后执行通知对账。

### 7.2 本地通知

- 事项或提醒写入 SQLite 成功后计算未来提醒，并通过 `expo-notifications` 调度。
- 保存稳定的逻辑提醒键与原生通知 ID 映射，重复对账不得重复调度。
- 完成、删除、修改时间或修改提醒时撤销失效通知。
- 按滚动窗口预调度有限数量的未来提醒，避免无限重复系列造成资源膨胀。
- 通知内容默认不展示备注；锁屏是否展示标题由后续隐私设置决定，默认采用隐私文案。
- 点击通知通过本地 ID 打开事项；事项已删除时展示友好空状态。

### 7.3 Android 边界

- Android 13+ 按上下文请求通知运行时权限。
- V1 不声明或申请 `SCHEDULE_EXACT_ALARM` / `USE_EXACT_ALARM`。
- 使用系统允许的非精确本地调度；Doze、省电、权限或厂商策略可能延迟通知。
- 后台任务不能作为准时通知的唯一机制，产品不得承诺绝对送达或准点。
- 本地通知失败不得影响事项数据保存；设置页提供诊断和测试入口。

---

## 8. 隐私、安全与错误状态

- V1 不采集账号信息，不把事项发送到 WakeWake 服务端。
- 日志、分析和崩溃报告不得包含标题、备注、分类名称或 JSON 备份内容。
- 备份解析必须限制文件大小和数据数量，并拒绝未知/不支持版本，避免部分恢复或资源耗尽。
- 导入不能执行文件中的任何代码或任意 SQL；只接受定义好的 JSON 字段。
- 本地数据库、通知 Payload 和临时备份只保存完成产品功能所需的最少信息。

必须设计以下状态：

- 首次使用且无事项、某日无事项、收集箱为空。
- 通知权限未决定、拒绝或被系统关闭。
- 本地通知调度失败，可重试但事项已安全保存。
- JSON 导出取消/失败、文件损坏、版本不支持、约束失败、导入回滚和导入成功。
- SQLite 迁移失败时阻止破坏性写入并提供可恢复提示。

不需要 V1 的状态包括：离线等待同步、账号会话过期、Push Token 失败、多设备冲突和服务端不可用。

---

## 9. 测试策略

### 9.1 Domain 与 Repository

优先覆盖：

- 事项字段和本地日期校验。
- 全天事项当地 09:00 提醒基准。
- 提醒偏移、过去提醒不补发和稳定逻辑键。
- 每日/每周 `interval = 1`、永不结束及非法规则拒绝。
- 系列锚点锁定、单次完成和实例键稳定性。
- DST 开始/结束、跨年、闰年及系统时区变化。
- SQLite CRUD、事务回滚和 Schema 迁移。

### 9.2 备份测试

- 导出 → 清空测试库 → 导入后的等价往返。
- 旧受支持 `schemaVersion` 的迁移。
- 未知版本、损坏 JSON、超大文件、重复 ID、非法日期和非法重复规则。
- 导入中途失败时完整回滚。
- 导入成功后通知只按当前数据重建一次。
- 导出不包含原生通知 ID、缓存、秘密或临时状态。

### 9.3 Android 真机

至少覆盖：

1. 无账号启动并创建、编辑、完成和恢复事项。
2. 在月、周、日、议程和收集箱中看到一致数据。
3. 创建每日及每周系列，只完成其中一次。
4. 验证全天事项以设备当地 09:00 为提醒基准。
5. 权限允许、拒绝、系统关闭及测试通知。
6. 创建、编辑、完成、删除后正确调度或撤销通知。
7. 前台、后台、进程终止、设备重启、Doze 和时区变化后的通知对账。
8. 导出 JSON、替换本地数据并成功恢复。
9. 损坏或不兼容备份不会改变现有数据。
10. 应用在飞行模式下完整完成核心流程。

验证通知规则和对账正确性，不以毫秒级准点作为验收标准。

---

## 10. 分阶段构建计划

### 阶段 0：工程与决策基线

- 保持 pnpm Monorepo、Expo、TypeScript、Lint、格式化和 CI。
- 采纳个人本地优先与时间/重复/通知 ADR。
- 保留 Supabase 骨架，但解除 V1 对 Docker、账号和后端的依赖。

退出标准：文档无范围冲突，JS/TS 检查通过，Android 环境要求明确。

### 阶段 1：单用户本地数据闭环

- 建立 SQLite Schema、迁移和 Repository。
- 实现分类、设置和事项 CRUD。
- 用本地数据替换演示数据。

退出标准：飞行模式下重启应用后数据仍完整，核心操作具有事务测试。

### 阶段 2：日历与受控重复

- 完成月、周、日、议程和收集箱数据映射。
- 实现每日/每周固定 interval 的按需展开。
- 实现实例完成记录和系列锚点锁定交互。

退出标准：所有视图一致，非法规则无法经 UI 或导入进入数据库，边界测试通过。

### 阶段 3：本地通知

- 完成默认模板、事项级多提醒、权限引导和测试通知。
- 完成稳定键、滚动调度、撤销和前台对账。
- 验证全天当地 09:00、系统时区变化和 Android 非精确调度边界。

退出标准：真机关键矩阵通过，无需精确闹钟权限，失败不造成事项丢失。

### 阶段 4：JSON 备份恢复

- 定义版本化格式、导出过滤规则和兼容策略。
- 实现系统文件导出、严格导入校验、确认和事务恢复。
- 导入成功后重建通知，失败时保持原数据不变。

退出标准：往返、损坏文件、旧版本迁移、回滚和敏感字段排除测试通过。

### 阶段 5：Android 稳定性与发布

- 完成性能、无障碍、隐私和 E2E 检查。
- 准备 Android 内部测试与发布资料。
- 明确通知非绝对准时和用户备份责任。

退出标准：Android 关键流程无 P0/P1 缺陷，发布清单完成，Supabase 不参与 V1 可用性。

---

## 11. V1 验收标准

1. 用户无需账号和网络即可进入并使用应用。
2. SQLite 是唯一业务事实来源，应用重启后数据完整。
3. 用户可创建、查看、编辑、完成、恢复和删除各类事项。
4. 月、周、日、议程和收集箱中的日期、时间及状态一致。
5. 全天事项保持本地日期，提醒基准固定为事项日期的设备当地 09:00。
6. 重复只接受每日或每周、`interval = 1`、永不结束。
7. 系列时间锚点锁定；完成或编辑单次实例不改变系列及历史实例键。
8. 每项支持 0 至 10 条提醒，过去提醒不补发。
9. 完成、取消、删除或改期后不再触发失效提醒。
10. Android 未授予通知权限时核心功能仍可使用，并有清晰恢复入口。
11. 应用不申请精确闹钟权限，文案不承诺绝对准时。
12. 用户能导出版本化 JSON，并能在确认后事务性恢复。
13. 损坏、超限或不兼容备份不会部分覆盖现有数据。
14. 导入后本地通知根据恢复后的数据正确重建且不重复。
15. 事项正文和备份内容不进入日志、分析或远端服务。
16. Android 真机关键流程、类型检查、Lint、格式检查和自动化测试通过。
17. 账号、Supabase 同步和远程推送未进入 V1 运行链路。
18. `supabase/` 骨架继续保留，可供后续版本设计与迁移使用。

---

## 12. 开发完成定义

每个 V1 功能任务完成前必须：

- 行为符合本文档和 ADR；变更决策时先更新 ADR。
- 包含正常、空、权限拒绝和本地错误状态。
- 通过 TypeScript 严格检查、Lint、格式检查和相关测试。
- 业务规则、SQLite 事务、备份格式或通知对账变更具有自动化测试。
- UI 检查浅色、深色、动态字体、触控尺寸和屏幕阅读器标签。
- 不在日志中输出事项正文、备份内容或其他私密数据。
- 涉及通知、文件或 SQLite 原生能力时在 Android 真机验证。
- 不为 V1 引入账号、Supabase 运行时、远程推送或精确闹钟权限依赖。

> 实施原则：先完成单机可测试的纵向闭环，再扩展视图；先锁定时间与重复语义，再实现通知；用户数据先落 SQLite，备份由用户掌控；为未来云能力保留迁移空间，但不让未来复杂度阻塞个人日用 V1。
